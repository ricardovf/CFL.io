/**
 * Returns the recursions for all non terminals in the form: [{ A: 'direct' }, { B: 'indirect' }]
 * @param grammar
 * @return {Array}
 */
export function getLeftRecursions(grammar) {
  return [];
}
